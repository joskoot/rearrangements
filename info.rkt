#lang setup/infotab
(define scribblings (list (list "rearrangements.scrbl")))
(define name "planet-fmt")
(define blurb (list "rearrangements"))
(define primary-file "rearrangements.rkt")
(define categories (list 'rearrangement 'permutation))
